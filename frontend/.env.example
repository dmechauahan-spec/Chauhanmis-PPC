# Base URL of the ppc-backend API (including the /api prefix). The backend
# defaults to PORT=3000 when its own .env doesn't set one — confirm against
# your actual backend .env/server.ts if it's different.
VITE_API_BASE_URL="http://localhost:3000/api"
